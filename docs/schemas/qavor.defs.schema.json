{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qavor.dev/schemas/qavor.defs.schema.json",
  "title": "qavor shared definitions",
  "description": "Shared $defs referenced by every kind-specific qavor manifest schema. Has no top-level shape of its own; consume via $ref to /$defs/<name>.",
  "type": "object",
  "$defs": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
      "description": "Lowercase identifier. Letters, digits, dot, dash, underscore. 1-63 chars. Used as service/profile/repo/group identifiers."
    },
    "envKey": {
      "type": "string",
      "pattern": "^[A-Z_][A-Z0-9_]*$",
      "description": "Conventional UPPER_SNAKE_CASE environment variable name."
    },
    "envScalar": {
      "description": "Scalar value usable on the right-hand side of an env entry. Strings support ${VAR} and ${secret:NAME} interpolation.",
      "oneOf": [{ "type": "string" }, { "type": "number" }, { "type": "boolean" }]
    },
    "envSpec": {
      "description": "Long-form env entry. Use when you need typing, validation, default vs override, secret marking, or documentation.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "value": { "$ref": "#/$defs/envScalar" },
        "default": { "$ref": "#/$defs/envScalar" },
        "required": { "type": "boolean", "default": false },
        "type": {
          "type": "string",
          "enum": ["string", "int", "number", "bool", "url", "duration"]
        },
        "pattern": { "type": "string", "format": "regex" },
        "secret": { "type": "boolean", "default": false },
        "description": { "type": "string" }
      }
    },
    "envMap": {
      "description": "Map of env names to scalar values or long-form envSpec entries.",
      "type": "object",
      "patternProperties": {
        "^[A-Z_][A-Z0-9_]*$": {
          "oneOf": [{ "$ref": "#/$defs/envScalar" }, { "$ref": "#/$defs/envSpec" }]
        }
      },
      "additionalProperties": false
    },
    "envBlock": {
      "description": "Layered env block. `common` always applies; `native` or `docker` is layered on top depending on the active run mode. `publish` (optional) is the explicit contract a backing service exposes to its dependents at start time — when present, dependents receive only the published keys instead of the service's full composed env. See the proposal section on Manifest Resolution Order for the full precedence chain.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "common": { "$ref": "#/$defs/envMap" },
        "native": { "$ref": "#/$defs/envMap" },
        "docker": { "$ref": "#/$defs/envMap" },
        "publish": { "$ref": "#/$defs/envMap" }
      }
    },
    "runtimeStep": {
      "description": "A single step in a command's `operations`. A step carries exactly one step kind: a shell `cmd` step (the classic form, written as a bare `{ cmd, cwd?, env?, shell? }` object), a declarative `compose` step (`{ compose: { action, … } }`, runs `docker compose`), or a declarative `docker` step (`{ docker: { action, name, … } }`, manages a single container). Steps are the atoms that make up a command's `operations` list; they run in declaration order and the first failure aborts the rest.",
      "oneOf": [
        { "$ref": "#/$defs/cmdStep" },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["compose"],
          "properties": { "compose": { "$ref": "#/$defs/composeStep" } }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["docker"],
          "properties": { "docker": { "$ref": "#/$defs/dockerStep" } }
        }
      ]
    },
    "cmdStep": {
      "description": "A shell step: one `cmd` plus optional `cwd`/`env`/`shell`. `${VAR}` references in `cmd` are expanded by the shell at run time against the composed service env.",
      "type": "object",
      "additionalProperties": false,
      "required": ["cmd"],
      "properties": {
        "cmd": {
          "type": "string",
          "description": "Shell command. Multiline strings are treated as a script."
        },
        "cwd": {
          "type": "string",
          "description": "Working directory relative to the defining manifest (the profile's own directory when the step comes from a profile)."
        },
        "env": { "$ref": "#/$defs/envMap" },
        "shell": {
          "type": "string",
          "description": "Override shell. Defaults to `/bin/sh -c` (POSIX) or `cmd /C` on Windows."
        }
      }
    },
    "composeStep": {
      "description": "Declarative `docker compose` step. qavor interpolates `${VAR}` in every string field from the composed service env (fail-closed on unresolved names), resolves `file`/`env_file`/`cwd` against the defining manifest's directory (the profile's own directory when the step comes from a profile — remote profile directories are materialized locally), and shells out to `docker compose`. The composed env is passed to the process, so the compose file itself may use `${VAR}` interpolation natively.",
      "type": "object",
      "additionalProperties": false,
      "required": ["action"],
      "properties": {
        "action": {
          "type": "string",
          "enum": ["up", "down", "stop", "start", "restart", "ps", "logs", "pull", "build"],
          "description": "Compose action. `up` always runs detached (`-d`); `down` keeps named volumes unless `volumes: true`."
        },
        "file": {
          "description": "Compose file path(s) relative to the defining manifest's directory. Defaults to `./docker-compose.yaml`. A list maps to repeated `-f` flags.",
          "oneOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" }, "minItems": 1 }
          ]
        },
        "project": {
          "type": "string",
          "description": "Compose project name (`-p`). Defaults to `qavor-<service-name>`."
        },
        "profiles": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Compose profiles to enable (repeated `--profile`)."
        },
        "services": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Scope the action to these compose services (appended after the action's flags)."
        },
        "env_file": {
          "description": "Extra env file path(s) relative to the defining manifest's directory (repeated `--env-file`).",
          "oneOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" }, "minItems": 1 }
          ]
        },
        "wait": {
          "type": "boolean",
          "description": "up: wait until services are running/healthy (`--wait`)."
        },
        "timeout": {
          "description": "Seconds. up: `--wait-timeout`; down/stop/restart: `--timeout`.",
          "oneOf": [{ "type": "number" }, { "type": "string" }]
        },
        "build": {
          "type": "boolean",
          "description": "up: build images before starting (`--build`)."
        },
        "force_recreate": {
          "type": "boolean",
          "description": "up: recreate containers even if unchanged (`--force-recreate`)."
        },
        "remove_orphans": {
          "type": "boolean",
          "description": "up/down: remove containers for services no longer in the file (`--remove-orphans`)."
        },
        "volumes": {
          "type": "boolean",
          "description": "down: also remove named volumes (`-v`) — destroys data."
        },
        "tail": {
          "description": "logs: number of trailing lines (`--tail`).",
          "oneOf": [{ "type": "number" }, { "type": "string" }]
        },
        "follow": {
          "type": "boolean",
          "description": "logs: follow output (`-f`). Blocks until interrupted; use with `--serial --verbose`."
        },
        "args": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Raw extra arguments appended after the action's flags — escape hatch for anything not modeled above."
        },
        "cwd": {
          "type": "string",
          "description": "Working directory relative to the defining manifest. Defaults to the defining manifest's directory."
        }
      }
    },
    "dockerStep": {
      "description": "Declarative single-container `docker` step. qavor interpolates `${VAR}` in every string field from the composed service env (fail-closed) and shells out to `docker`. `up` is idempotent ensure-running: `docker run -d` if the container is absent, `docker start` if stopped, no-op if already running. `down` stops and removes the container; named volumes are kept unless listed in `remove_volumes`.",
      "type": "object",
      "additionalProperties": false,
      "required": ["action", "name"],
      "properties": {
        "action": {
          "type": "string",
          "enum": ["up", "down", "run", "start", "stop", "restart", "rm", "logs", "status"],
          "description": "Container action. `up`/`down` are the idempotent lifecycle pair; `run`/`start`/`stop`/`restart`/`rm` are the raw docker primitives; `status` prints the container's current state."
        },
        "name": { "type": "string", "description": "Container name." },
        "image": {
          "type": "string",
          "description": "Image reference for `up`/`run`. Pin exact tags — avoid floating `latest`."
        },
        "ports": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Port mappings (repeated `-p host:container`)."
        },
        "env": {
          "type": "object",
          "additionalProperties": {
            "oneOf": [{ "type": "string" }, { "type": "number" }, { "type": "boolean" }]
          },
          "description": "Container environment variables (repeated `-e`)."
        },
        "volumes": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Volume mounts (repeated `-v`, e.g. `myvol:/data`)."
        },
        "network": { "type": "string", "description": "Network to attach to (`--network`)." },
        "entrypoint": {
          "type": "string",
          "description": "Override the image entrypoint (`--entrypoint`, a single executable per the docker CLI)."
        },
        "command": {
          "description": "Command run after the image name. A string is split on whitespace; use the list form for arguments containing spaces.",
          "oneOf": [{ "type": "string" }, { "type": "array", "items": { "type": "string" } }]
        },
        "user": { "type": "string", "description": "`--user`." },
        "workdir": { "type": "string", "description": "`--workdir`." },
        "restart": {
          "type": "string",
          "description": "Restart policy (`--restart`), e.g. `no`, `always`, `unless-stopped`, `on-failure`."
        },
        "labels": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Container labels (repeated `--label key=value`)."
        },
        "platform": { "type": "string", "description": "`--platform`, e.g. `linux/amd64`." },
        "pull": {
          "type": "string",
          "enum": ["always", "missing", "never"],
          "description": "Image pull policy for `run` (`--pull`)."
        },
        "detach": {
          "type": "boolean",
          "default": true,
          "description": "run/up: run detached (`-d`). Almost always true for services."
        },
        "healthcheck": {
          "type": "object",
          "additionalProperties": false,
          "description": "Container healthcheck flags. `wait: true` polls this health status.",
          "properties": {
            "test": { "type": "string", "description": "Health command (`--health-cmd`)." },
            "interval": { "type": "string", "description": "`--health-interval`, e.g. `2s`." },
            "retries": {
              "description": "`--health-retries`.",
              "oneOf": [{ "type": "number" }, { "type": "string" }]
            },
            "timeout": { "type": "string", "description": "`--health-timeout`." },
            "start_period": { "type": "string", "description": "`--health-start-period`." }
          }
        },
        "wait": {
          "type": "boolean",
          "description": "up/run: after starting, poll the container until healthy (or simply running when it defines no healthcheck)."
        },
        "timeout": {
          "description": "Seconds. Wait-poll deadline for `wait: true`; grace period for stop/restart (`-t`).",
          "oneOf": [{ "type": "number" }, { "type": "string" }]
        },
        "remove_volumes": {
          "type": "array",
          "items": { "type": "string" },
          "description": "down/rm: also `docker volume rm -f` these named volumes — destroys data."
        },
        "tail": {
          "description": "logs: number of trailing lines (`--tail`).",
          "oneOf": [{ "type": "number" }, { "type": "string" }]
        },
        "follow": {
          "type": "boolean",
          "description": "logs: follow output (`-f`). Blocks until interrupted; use with `--serial --verbose`."
        },
        "args": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Raw extra arguments spliced in before the image (`run`) or container name — escape hatch."
        }
      }
    },
    "runtimeStepOrList": {
      "description": "A step value used inside a command's `operations`: either a single step object, or a list of step objects run in sequence. Each list entry is a full step of any kind (`cmd`, `compose`, or `docker`); steps run in declaration order and the first non-zero exit aborts the rest. The single-object form (`operations: { cmd: \"…\" }`) and the list form (`operations: [{ cmd: \"…\" }, { compose: { action: \"up\" } }]`) are interchangeable.",
      "oneOf": [
        { "$ref": "#/$defs/runtimeStep" },
        {
          "type": "array",
          "items": { "$ref": "#/$defs/runtimeStep" },
          "minItems": 1
        }
      ]
    },
    "runtimeMergeDirective": {
      "description": "Merge directive controlling how a command's `operations` combine with the same command inherited from a referenced profile. Written as the value of `operations`. Exactly one of `$append`/`$prepend`/`$replace`/`$unset` may be set. `$append`/`$prepend` splice the given step(s) after / before the inherited steps; `$replace` overrides them entirely (the same effect as a bare list, stated explicitly); `$unset: true` drops the inherited command entirely. On a command with no inherited value, `$append`/`$prepend`/`$replace` simply yield the given step(s) and `$unset` is a no-op.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "$append": { "$ref": "#/$defs/runtimeStepOrList" },
        "$prepend": { "$ref": "#/$defs/runtimeStepOrList" },
        "$replace": { "$ref": "#/$defs/runtimeStepOrList" },
        "$unset": { "const": true }
      },
      "oneOf": [
        { "required": ["$append"] },
        { "required": ["$prepend"] },
        { "required": ["$replace"] },
        { "required": ["$unset"] }
      ]
    },
    "runtimeStepOrMerge": {
      "description": "The value of a command's `operations`: either a single step or a list of steps (like `runtimeStepOrList`), or a `runtimeMergeDirective` object (`$append`/`$prepend`/`$replace`/`$unset`) that controls how these steps merge with the same command inherited from a referenced profile. The bare step / list form keeps the default behaviour (replace the inherited value).",
      "oneOf": [
        { "$ref": "#/$defs/runtimeStep" },
        {
          "type": "array",
          "items": { "$ref": "#/$defs/runtimeStep" },
          "minItems": 1
        },
        { "$ref": "#/$defs/runtimeMergeDirective" }
      ]
    },
    "runtimeDescribedCommand": {
      "description": "A runtime command. Every command on a backend — the reserved install-lifecycle commands `check_installed` and `install`, and every user-defined command (`run`, `prepare`, `update_libraries`, `lint`, `test`, `migrate`, …) — uses this one uniform shape: an optional one-line `description` plus the `operations` that run. `operations` is a single step, a list of steps run in declaration order (the first non-zero exit aborts the rest), or a profile-merge directive (`$append`/`$prepend`/`$replace`/`$unset`) that extends or drops a value inherited from a referenced profile.",
      "type": "object",
      "additionalProperties": false,
      "required": ["operations"],
      "properties": {
        "description": {
          "type": "string",
          "description": "One-line human-readable description of what this command does. Surfaced by `qavor commands` and by `qavor <command> --help`; has no effect on execution."
        },
        "operations": { "$ref": "#/$defs/runtimeStepOrMerge" }
      }
    },
    "runtimeBackend": {
      "description": "Runtime backend definition. `enabled` (a boolean gate) is the only non-command key. Every other key is a command that shares one uniform shape (`runtimeDescribedCommand`: an optional `description` plus `operations`) — there is no difference in structure between the reserved lifecycle commands and user-defined ones. Only two commands are reserved, both for the install lifecycle: `check_installed` and `install` (`install` runs only when `check_installed` fails). Every *other* key is a user-defined command discovered and run on demand by `qavor <command>` (e.g. `run`, `prepare`, `update_libraries`, `lint`, `test`, `migrate`); qavor assumes no fixed command set and treats them all identically, fanning each out across the services that declare it. A command's `operations` accepts a single step, a list of steps run in declaration order (first non-zero exit aborts the rest), or a profile-merge directive (`$append`/`$prepend`/`$replace`/`$unset`) that extends or drops steps inherited from a referenced profile.",
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean", "default": false },
        "check_installed": { "$ref": "#/$defs/runtimeDescribedCommand" },
        "install": { "$ref": "#/$defs/runtimeDescribedCommand" }
      },
      "additionalProperties": { "$ref": "#/$defs/runtimeDescribedCommand" }
    },
    "runtimeBlock": {
      "description": "Available runtime backends. A service manifest may declare any subset; the active backend is chosen by the resolved `mode`.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "native": { "$ref": "#/$defs/runtimeBackend" },
        "docker": { "$ref": "#/$defs/runtimeBackend" },
        "docker-compose": { "$ref": "#/$defs/runtimeBackend" }
      }
    },
    "mode": {
      "description": "Run mode. `docker-compose` is intended for backing services managed via a generated compose project (ADR-005).",
      "type": "string",
      "enum": ["native", "docker", "docker-compose"]
    },
    "profileRef": {
      "description": "Reference to a profile. Either a bare workspace-local name, a remote source URI (https / GitHub / git SSH or HTTPS / file), or a long-form object carrying the source plus optional pin and auth. Profiles are merged in declaration order; later entries win.",
      "oneOf": [
        { "$ref": "#/$defs/name" },
        { "$ref": "#/$defs/profileSourceUri" },
        { "$ref": "#/$defs/profileSource" }
      ]
    },
    "profileSourceUri": {
      "type": "string",
      "description": "Remote profile source as a single string. Supported forms: an https URL to a YAML profile document; a GitHub blob/raw URL or `github:<owner>/<repo>//<path>[@<ref>]` shorthand; a git repo ref `git@host:owner/repo.git//<path>[@<ref>]` (or `ssh://` / `https://….git//<path>`); or a `file://` path. A path that does not end in `.yaml`/`.yml` is a **directory reference**: the profile is read from `<path>/qavor.yaml` and the entire directory (compose files, configs, …) is materialized locally so the profile's steps can reference sibling files. Directory references are supported for GitHub (`github:` shorthand or a `/tree/<ref>/` page URL), git, and file sources — not for plain https URLs, which cannot be enumerated. An optional `#sha256=<64 hex>` fragment pins the fetched profile document (`qavor.yaml` for directory references).",
      "pattern": "^(https?://|git@|git://|ssh://|file://|github:).+"
    },
    "profileSource": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url"],
      "description": "Long-form remote profile reference: a source URL plus optional git ref, integrity pin, expected name, and auth.",
      "properties": {
        "name": {
          "$ref": "#/$defs/name",
          "description": "Expected profile name. When set, the fetched profile's own `name` must match; it is also used as the local reference name."
        },
        "url": {
          "type": "string",
          "description": "Source URI. Same forms as a `profileSourceUri` string (https / GitHub / git / file), including directory references (a path not ending in `.yaml`/`.yml` reads `<path>/qavor.yaml` and materializes the whole directory)."
        },
        "ref": {
          "type": "string",
          "description": "Git ref (branch, tag, or commit) for git sources. Ignored for plain https/file sources."
        },
        "integrity": {
          "type": "string",
          "pattern": "^sha256-[A-Fa-f0-9]{64}$",
          "description": "Optional Subresource-Integrity-style pin. When present the fetched content's sha256 must match, or resolution fails closed."
        },
        "auth": { "$ref": "#/$defs/profileAuth" }
      }
    },
    "profileAuth": {
      "type": "object",
      "additionalProperties": false,
      "description": "Authentication for remote profile sources. Git sources authenticate through the user's git credential helper / SSH agent and need nothing here; https/GitHub sources may set a bearer token.",
      "properties": {
        "tokenEnv": {
          "$ref": "#/$defs/envKey",
          "description": "Name of an environment variable holding a bearer token, sent as `Authorization: Bearer <value>` on https/GitHub fetches."
        }
      }
    },
    "requirement": {
      "description": "A single dependency edge. Exactly one of `service` or `group` must be set. Backing services (postgres, kafka, …) are referenced the same way as any other service.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "service": {
          "type": "string",
          "description": "Service reference. `<service>` for same-workspace, `<repo>:<service>` permitted."
        },
        "group": { "$ref": "#/$defs/name" },
        "optional": { "type": "boolean", "default": false },
        "condition": {
          "type": "string",
          "description": "Optional gating expression. Examples: `mode == 'docker'`, `profile == 'dev'`, `os == 'darwin'`."
        },
        "waitFor": {
          "type": "string",
          "enum": ["start", "ready"],
          "default": "ready",
          "description": "Whether to wait for process-up or for the readiness probe to pass before starting dependents."
        }
      },
      "oneOf": [{ "required": ["service"] }, { "required": ["group"] }]
    },
    "hookCommands": {
      "description": "One or more shell commands or paths to executable scripts, run in the manifest's directory.",
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" }, "minItems": 1 }
      ]
    },
    "hooks": {
      "description": "Lifecycle hooks. Each hook list runs in the manifest's directory at the corresponding lifecycle event. `pre_command`/`post_command` fire around every user-defined `qavor <command>` run; qavor exposes `QAVOR_COMMAND` (the running command name), `QAVOR_WORKSPACE_DIR`, `QAVOR_PROJECT_DIR`, and `QAVOR_SERVICE_DIR` (absolute workspace/project/service paths) to the hook script and to each command step.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "pre_clone": { "$ref": "#/$defs/hookCommands" },
        "post_clone": { "$ref": "#/$defs/hookCommands" },
        "pre_command": { "$ref": "#/$defs/hookCommands" },
        "post_command": { "$ref": "#/$defs/hookCommands" },
        "pre_run": { "$ref": "#/$defs/hookCommands" },
        "post_run": { "$ref": "#/$defs/hookCommands" },
        "pre_stop": { "$ref": "#/$defs/hookCommands" },
        "post_stop": { "$ref": "#/$defs/hookCommands" }
      }
    },
    "schemaVersion": {
      "description": "Manifest schema version. Defaults to 1 if omitted.",
      "type": "integer",
      "enum": [1]
    }
  }
}
